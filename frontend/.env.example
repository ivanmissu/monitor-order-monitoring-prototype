# Optional: override the browser-facing API base. Leave empty when the same host/gateway proxies /api/v1.
# VITE_API_BASE=https://monitor.example.com

# Optional: override the Vite development proxy target for /api.
# MONITOR_API_TARGET=http://127.0.0.1:8080
